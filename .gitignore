.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/
.coverage
htmlcov/
build/
dist/
*.so
.python-version-local

# Local-only profiler + benchmark outputs.
scripts/bench-results/
*.prof

# Microbench build artefacts.
scripts/microbench/target/

# Local agent-harness settings (never commit).
.claude/

# Raw cloud counts-run scratch (kept off-repo like the N=29 binaries;
# the curated certificate lives at docs/results/nNN.json).
n30-ship/
n30_ship.log
