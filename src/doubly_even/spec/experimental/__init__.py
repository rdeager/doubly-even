"""Experimental / deferred-direction spec primitives.

Holds spec-layer primitives that exist only as scaffolding for a
deferred direction. Currently houses the BHM2012 direct-sum operator
(`direct_sum.py`) that feeds the mass-seeding prototype in
``..enumerate.experimental.seeds``.

See ``/workspace/src/EXPERIMENTAL.md`` and the memory bullet
``project_paper_2012_audit.md`` for the broader context.
"""
