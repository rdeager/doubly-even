"""Experimental / dormant canonicaliser implementations.

Kept in tree as reference oracles, audit substrate, and prototypes for
future optimisation attempts. **Not on the active hot path** — the
spine dispatcher in ``doubly_even.canon.nauty`` only reaches these
modules through opt-in env-var routes (``DOUBLY_EVEN_CANON_BACKEND``).

See ``/workspace/src/EXPERIMENTAL.md`` for what each module is and the
memory bullet that contains its post-mortem.
"""
